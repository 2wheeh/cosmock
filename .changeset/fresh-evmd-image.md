---
'starskiff': patch
---

Upgrade the starskiff-published evmd image source to cosmos/evm v0.7.1. The default uses the write-once v0.7.1 tag until the follow-up image publish records and pins its multi-arch manifest digest.
