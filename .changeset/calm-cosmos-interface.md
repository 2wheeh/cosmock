---
'starskiff': patch
---

Replace the internal Cosmos execution helper with an optional `runtime` extension for custom chain definitions while keeping high-level Instance parameters domain-specific.
