---
"starskiff": patch
---

Add `privacyZkArtifacts` to `Instance.marood()` so maroo v0.8+ can use generated-test or release Privacy ZK artifacts from a host directory across every Docker and local-binary lifecycle command.
