---
'starskiff': patch
---

Make Cosmos bootstrap commands cancellable across local-binary and Docker runtimes, replace blocking mnemonic recovery, and reject clean node exits that happen before readiness.
