---
'starskiff': patch
---

Make high-level chain instance parameters reject simultaneous `image` and `binary` sources in TypeScript, and require one source for `marood` while preserving runtime validation for JavaScript callers.
