---
'starskiff': patch
---

Prevent retries from racing with cleanup after an instance start timeout, expose the timeout abort signal to instance definitions, and preserve custom property descriptors.
