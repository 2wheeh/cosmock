# Container Runtime [Run nodes from version-pinned chain images]

Chains with a usable image are **container-first**: the instance runs that image out of the box. When a chain only publishes release binaries, Starskiff may package those exact checksummed artifacts instead. No Go toolchain or manual build is needed.

```ts
import { Instance } from 'starskiff';

// Boots Starskiff's image containing XPLA's official release binary
const instance = Instance.xplad({
  accounts: [{ mnemonic: '...', coins: '1000000000000000000000axpla' }],
});
await instance.start();
```

Docker must be running. The image is pulled on first use and cached by Docker afterward.

## This is not orchestration

starskiff does not become Kubernetes-shaped when it runs a container. The image is only **where the node comes from** — everything else is unchanged:

- The chain CLI (`init`, `keys add`, `gentx`, …) runs in disposable containers against a host temp directory that's bind mounted as the node's home.
- **Genesis and config patching stays plain host-side file I/O.** The container and starskiff read the same bytes.
- The node runs **attached** as a child process, so `stdout`/`stderr` streaming, the message buffer, events, and exit detection are identical to the binary runtime.
- Ports are published 1:1, so `rpcUrl` / `apiUrl` / `evmUrl` and every cosmjs or viem client work exactly as before.
- `stop()` kills the process, force-removes the container, and deletes the temp home directory.

There is no Helm, no cluster, no pod scheduling — just `docker run` in place of `exec`.

## Escape hatches

Not every chain ships an image, and sometimes you need your own build. Two ways out:

```ts
// 1. Bind your own image (a fork, a local build, a private registry)
Instance.simd({ image: 'my-registry/simd:custom' });

// 2. Use a binary on PATH — the classic runtime
Instance.simd({ binary: 'simd' });
```

Naming a `binary` opts out of the container runtime entirely. The two choices
are mutually exclusive: TypeScript rejects calls that pass both `image` and
`binary`, and the runtime keeps the same validation for JavaScript callers.

For instances with **no default image** ([`marood`](/docs/chains)) these aren't
overrides but a requirement. TypeScript requires exactly one when constructing
the instance, while JavaScript callers get the same fail-fast runtime error if
they omit both.

```ts
Instance.marood({ image: 'my-registry/marood:private' }); // or { binary: 'marood' }
```

## Where the image comes from

An instance is container-first when it can source a node from an image. That image comes from one of two places:

1. **The chain's own official image**, when it exists and tracks the live network — e.g. MANTRA runs `ghcr.io/mantra-chain/mantrachain`.
2. **An image starskiff builds or packages and publishes**, when the chain ships no usable image — e.g. `evmd` is built from pinned cosmos/evm source, while `xplad` wraps XPLA's checksummed official release binaries.

| Instance                       | Default runtime | Source                                                  |
| ------------------------------ | --------------- | ------------------------------------------------------- |
| [`simd`](/docs/instances/simd) | Container       | Official `ghcr.io/cosmos/simapp` image (minor-line pin) |
| `wasmd`                        | Container       | Official `cosmwasm/wasmd` image, exact version tags     |
| `xplad`                        | Container       | **starskiff-published** image from official XPLA binaries |
| `evmd`                         | Container       | **starskiff-published** image (cosmos/evm ships none)   |
| [`gaiad`](/docs/chains)        | Container       | Official `ghcr.io/cosmos/gaia` image, tracks mainnet    |
| [`mantra`](/docs/chains)       | Container       | Official `ghcr.io/mantra-chain/mantrachain` image       |
| [`xrplevm`](/docs/chains)      | Container       | Official `peersyst/exrp` image (amd64-only)             |
| `hermes`                       | Binary          | IBC relayer, run as a host process                      |

`hermes` is the one deliberate binary default — a relayer that drives its own CLI and config on the host, not a chain node the container runtime boots; starskiff's own CI provisions it via [`config/binaries.json`](https://github.com/2wheeh/starskiff/blob/main/config/binaries.json). [`marood`](/docs/chains) has **no default at all** — its node source is private — so it requires an injected `image` or `binary` and is never in that manifest. starskiff doesn't publish an image for every missing/stale upstream — only the deliberate allowlist in `config/images.json` (`evmd` and `xplad`).

### Images we publish

For chains with no usable upstream image, starskiff builds or packages a multi-arch (amd64 + arm64) image and pushes it to its **public** GHCR namespace, `ghcr.io/2wheeh/starskiff/<chain>`. Public GHCR packages are free to store and pull, so this stays within the free tier.

The list of what we publish is an explicit allowlist in [`config/images.json`](https://github.com/2wheeh/starskiff/blob/main/config/images.json) — the single source of truth for both the publish workflow and the instance's default image. A dedicated `publish-images` workflow builds each entry; targets are never derived from the instance list, so a chain is only ever redistributed by a deliberate edit.

Publishing a chain image ourselves means we redistribute someone else's node, so source builds are pinned to an exact upstream ref + commit, release assets are checksum-verified, and defaults are pinned to the multi-arch manifest **digest**, with build provenance and an SBOM attached.

## CI

Both runtimes work in CI. The container runtime removes the binary-provisioning step entirely — no build job, no release assets to keep in sync — at the cost of an image pull per cold run. See the [CI guide](/docs/guides/ci).

```yaml
- run: pnpm vitest run # docker is preinstalled on GitHub runners
```

## Caveats

- **Docker becomes a hard dependency** for container-backed instances. On a machine without a running daemon, `start()` fails with an actionable error telling you to pass a `binary` instead.
- **First run pays the pull** (a few hundred MB). The pull is part of `start()`, reports progress through instance messages, obeys the start timeout, and is cached afterward.
- On macOS, containers run in a VM, so filesystem I/O is slower than a native binary. Boot is still seconds, not minutes.
